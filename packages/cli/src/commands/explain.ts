/**
 * `earsyntax explain <diagnostic-id>` — full write-up of one diagnostic,
 * resolving current ids and deprecated aliases. Reimplemented by the host-native
 * explain agent; this is a compiling stub until then.
 */

import { notImplemented } from './stub.js';

export const explainCommand = notImplemented('explain');
