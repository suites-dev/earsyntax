/**
 * `earsyntax instructions <author|convert|repair|review> --file <path> [--from <source>]`
 * — return the deterministic rules an agent follows for one loop step against a
 * host file. Reimplemented by the host-native instructions agent; this is a
 * compiling stub until then.
 */

import { notImplemented } from './stub.js';

export const instructionsCommand = notImplemented('instructions', true);
