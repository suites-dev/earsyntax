/**
 * `earsyntax init --agent <agents> --host <hosts>` — render managed agent-wrapper
 * and host-integration files. Reimplemented by the host-native init agent; this
 * is a compiling stub until then.
 */

import { notImplemented } from './stub.js';

export const initCommand = notImplemented('init');
