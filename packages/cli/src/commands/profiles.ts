/**
 * `earsyntax profiles` — list the built-in profiles, rendered from profile data.
 * Reimplemented by the host-native profiles agent; this is a compiling stub
 * until then.
 */

import { notImplemented } from './stub.js';

export const profilesCommand = notImplemented('profiles');
