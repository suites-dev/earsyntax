/**
 * `earsyntax doctor` — detect host frameworks and agent integrations in the repo
 * at `--cwd` and recommend exact commands. Reimplemented by the host-native
 * doctor agent; this is a compiling stub until then.
 */

import { notImplemented } from './stub.js';

export const doctorCommand = notImplemented('doctor');
