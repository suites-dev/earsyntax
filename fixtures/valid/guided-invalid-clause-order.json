{
  "id": "VAL-037",
  "text": "When a payment webhook is received, while the payment provider is available, the billing service shall queue the event.",
  "options": {
    "mode": "guided"
  },
  "expected": {
    "valid": true,
    "pattern": "complex",
    "diagnostics": [
      { "code": "ears.invalid_clause_order", "severity": "warning" }
    ]
  }
}
