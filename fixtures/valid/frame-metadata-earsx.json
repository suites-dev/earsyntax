{
  "id": "VAL-FRAME",
  "text": "REQ-001 When a payment webhook is received, the billing service shall verify the HMAC signature. [source: specs/billing.md:12]",
  "options": { "dialect": { "allowFrameMetadata": true } },
  "expected": {
    "valid": true,
    "pattern": "event-driven",
    "diagnostics": [],
    "ast": { "pattern": "event-driven", "system": { "raw": "billing service", "role": "system" } },
    "responses": ["verify the HMAC signature"]
  }
}
